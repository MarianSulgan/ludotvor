/**
 * Module for Shape Grammar components
 * 
 * only interface-ish, wrapping them up
 */

import Canvas from './components/Canvas';

const ShapeGrammar = {
    Canvas: Canvas
}

export default ShapeGrammar;
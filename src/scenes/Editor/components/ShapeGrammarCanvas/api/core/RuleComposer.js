/**
 * composes rules for generation based on shapes selected by user
 * 
 * input: array of selected shapes (from user input in page scenes/SelectPattern)
 * output: ordered set of rules with mechanism for iterative selection of rules
 */
/**
 * adds actual data to shapeContainer and wraps
 * for use in frontend.
 * should have additional React component that takes
 * output and actually renders svg to "canvas"
 * 
 * input: Pattern
 * output: JSON
 */
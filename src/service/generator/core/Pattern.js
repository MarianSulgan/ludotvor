/**
 * array of shapeContainers
 * abstractly represents actual "canvas" before wrapping
 */

class Pattern {
    constructor(shapeContainers) {
        this.shapeContainers = shapeContainers;
    }
}